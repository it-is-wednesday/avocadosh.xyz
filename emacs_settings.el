(load "~/.config/emacs/.local/straight/repos/emacs-htmlize/htmlize.el" :NOMESSAGE t)

;; silence the "Can't guess python-indent-offset" message
(setq python-indent-guess-indent-offset t)
(setq python-indent-guess-indent-offset-verbose nil)
